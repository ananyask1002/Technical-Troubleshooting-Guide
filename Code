print("Program Running")
issues = {
    "1": {
        "name": "System Not Powering On",
        "steps": [
            "Check power cable connection.",
            "Check power socket.",
            "Turn ON SMPS switch.",
            "Check motherboard LED."
        ]
    },
    "2": {
        "name": "Internet Not Working",
        "steps": [
            "Check router power.",
            "Restart router.",
            "Check LAN cable.",
            "Run network diagnostics."
        ]
    }
}

def show_menu():
    print("\n===== TECHNICAL TROUBLESHOOTING GUIDE =====")
    for key in issues:
        print(f"{key}. {issues[key]['name']}")
    print("0. Exit")

def troubleshoot(choice):
    if choice in issues:
        print(f"\nTroubleshooting: {issues[choice]['name']}")
        for step_no, step in enumerate(issues[choice]['steps'], 1):
            input(f"\nStep {step_no}: {step}\nPress Enter after completing...")
        print("\nIssue Diagnosis Completed.")
    else:
        print("Invalid Option.")

while True:
    show_menu()
    choice = input("Select issue number: ")
    
    if choice == "0":
        print("Exiting...")
        break
    
    troubleshoot(choice)
